# ─────────────────────────────────────────────────────────────────────────────
# Voice Clone Project — Main Environment Requirements
# Install in the main virtual environment (venv):
#   python -m venv venv
#   venv\Scripts\activate       # Windows
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── Core web server ───────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9        # Required for FastAPI form/file uploads

# ── TTS / Voice Synthesis ─────────────────────────────────────────────────────
TTS>=0.22.0                    # Coqui TTS — includes XTTS v2

# ── Transcription ─────────────────────────────────────────────────────────────
faster-whisper>=1.0.0          # CTranslate2-based Whisper (faster, lower VRAM)

# ── Audio I/O & Processing ────────────────────────────────────────────────────
sounddevice>=0.4.6             # Microphone recording
soundfile>=0.12.1              # Audio file read/write (libsndfile)
scipy>=1.11.0                  # Signal processing (high-pass filter, etc.)
noisereduce>=2.0.0             # Spectral-subtraction noise reduction

# ── ML Backend ────────────────────────────────────────────────────────────────
# NOTE: Install PyTorch with CUDA support separately if you have a GPU:
#   pip install torch>=2.1.0 torchaudio>=2.1.0 --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24.0,<2.0             # Pinned below 2.0 for RVC/fairseq compatibility

# ── Transformers (required by Coqui TTS / XTTS v2) ───────────────────────────
transformers>=4.38.0
